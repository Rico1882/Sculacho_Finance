{
  "name": "sculacho-com",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Cockpit financeiro local-first com fluxo de caixa, orcamentos e relatorios.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:zip": "npm run build && bestzip dist.zip dist",
    "preview": "vite preview",
    "app": "vite preview --port 4173 --open --strictPort",
    "android:sync": "npm run build && cap sync android",
    "android:open": "cap open android",
    "android:debug": "npm run android:sync && cd android && gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "bestzip": "^2.2.3",
    "typescript": "~5.7.2",
    "vite": "^6.0.3",
    "xlsx": "^0.18.5"
  }
}
